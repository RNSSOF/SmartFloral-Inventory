{
  "name": "smartfloral-inventory",
  "version": "0.1.0",
  "description": "Smart inventory management system for flower shops with AI-powered recommendations",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run backend-dev\" \"npm run frontend-dev\"",
    "backend-dev": "cd backend && npm run dev",
    "frontend-dev": "cd frontend && npm start",
    "backend-start": "cd backend && node server.js",
    "frontend-build": "cd frontend && npm run build",
    "electron": "cd frontend && electron public/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "flower",
    "inventory",
    "management",
    "ai",
    "pricing"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
